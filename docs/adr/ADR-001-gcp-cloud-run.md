# ADR-001: GCPサーバーレス（Cloud Run）の採用

- ステータス: 承認済み
- 日付: 未定

## 背景
寄付はイベント時（投影）にスパイク（集中）が発生する一方、平時はアクセスが少ない。

## 決定
Cloud Runを採用する。

## 理由
- アクセス数に応じたオートスケーリングが可能
- 「使った分だけ」の課金（Pay-as-you-go）により運用コストを最小化できる

## 影響
- コンテナ化が前提となるため、DockerfileとCI/CD設計が必要
- 同期/非同期処理の分離設計が必要（Webhook受信など）
